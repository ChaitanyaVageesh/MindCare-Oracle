global:
  scrape_interval: 5s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "mindcare-api"
    static_configs:
      - targets: ["api:8000"]
    metrics_path: /metrics
